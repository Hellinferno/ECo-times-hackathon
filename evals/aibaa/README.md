# AIBAA evals

Use the shared gold datasets in `datasets/aibaa/*/gold.jsonl` to score:

- router accuracy
- retrieval recall
- extraction exact match
- validator fidelity
- citation coverage

Place generated JSON reports in `evals/aibaa/reports/` so the admin API can list and retrieve them.
